# MyBatis Mapper XML

MyBatis SQL Mapper XML 파일을 이 폴더에 둡니다.

- 예: `UserMapper.xml`
- Java Mapper 인터페이스는 `com.tripai.backend.repository` 패키지에 둡니다.
- SQL 결과의 `snake_case` 컬럼은 MyBatis 설정에 따라 Java의 `camelCase` 필드로 매핑됩니다.
